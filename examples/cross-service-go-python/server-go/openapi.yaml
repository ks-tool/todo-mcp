openapi: 3.0.0
info:
  title: users service (Go)
  version: "1.0"
paths:
  /users:
    post:
      operationId: createUser
      requestBody:
        content:
          application/json:
            schema:
              properties:
                name: {}
                email: {}
      responses:
        "201":
          content:
            application/json:
              schema:
                properties:
                  id: {}
